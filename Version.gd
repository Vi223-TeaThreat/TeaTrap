extends RefCounted
# НОМЕР СБОРКИ. Файл пишет хук `.githooks/pre-commit` перед каждым коммитом —
# руками не править, правка потеряется на следующем же коммите.

const NUMBER: int = 145
const DATE: String = "14.09.2026"
